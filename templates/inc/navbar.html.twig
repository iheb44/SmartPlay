<header>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand text-white" href="/"><i class="fas fa-gamepad"></i>SmartPlay</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nvbCollapse" aria-controls="nvbCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="nvbCollapse">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item pl-1 {{ app.request.get('_route') == "home" ? "active":"" }}">
                        <a class="nav-link" href="{{ path('home') }}"><i class="fa fa-home fa-fw mr-1"></i>home</a>
                    </li>
                    <li class="nav-item pl-1 {{ app.request.get('_route') == "partner" ? "active":"" }}">
                        <a class="nav-link" href="{{ path('partner') }}"><i class="fa fa-info-circle fa-fw mr-1"></i>partner</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    </header>
